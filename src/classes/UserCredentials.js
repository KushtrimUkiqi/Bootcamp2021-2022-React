export default class UserCredentials
{
    constructor(loggedIn,sessionID,message)
    {

        this.loggedIn = loggedIn;
        this.sessionID = sessionID;
        this.message = message;
    }
    
}